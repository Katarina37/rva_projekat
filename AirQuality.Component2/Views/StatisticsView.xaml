<UserControl x:Class="AirQuality.Component2.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="560"
             d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Statistika kvaliteta vazduha"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,16" />

        <WrapPanel Grid.Row="1" Margin="0,0,0,14">
            <StackPanel Margin="0,0,12,8">
                <TextBlock Text="Merna stanica" />
                <ComboBox Width="220"
                          ItemsSource="{Binding Stations}"
                          SelectedItem="{Binding SelectedStation}"
                          DisplayMemberPath="Name" />
            </StackPanel>

            <StackPanel Margin="0,0,12,8">
                <TextBlock Text="Mesec" />
                <ComboBox Width="80"
                          ItemsSource="{Binding Months}"
                          SelectedItem="{Binding SelectedMonth}" />
            </StackPanel>

            <StackPanel Margin="0,0,12,8">
                <TextBlock Text="Godina" />
                <TextBox Width="90"
                         Text="{Binding SelectedYear, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <Button Width="130"
                    Height="28"
                    Margin="0,18,0,8"
                    Content="Preuzmi podatke"
                    Command="{Binding LoadDataCommand}" />
        </WrapPanel>

        <TextBlock Grid.Row="2"
                   Text="Adaptirana merenja"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6" />

        <ListBox Grid.Row="3"
                 ItemsSource="{Binding DisplayReadings}"
                 Margin="0,0,0,14">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               TextWrapping="Wrap"
                               Margin="4" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <WrapPanel Grid.Row="4" Margin="0,0,0,14">
            <StackPanel Margin="0,0,12,8">
                <TextBlock Text="Statisticka metoda" />
                <ComboBox Width="220"
                          ItemsSource="{Binding Strategies}"
                          SelectedItem="{Binding SelectedStrategy}"
                          DisplayMemberPath="Name" />
            </StackPanel>

            <Button Width="100"
                    Height="28"
                    Margin="0,18,12,8"
                    Content="Izracunaj"
                    Command="{Binding CalculateCommand}" />

            <Button Width="100"
                    Height="28"
                    Margin="0,18,0,8"
                    Content="Izvezi CSV"
                    Command="{Binding ExportCsvCommand}" />
        </WrapPanel>

        <TextBlock Grid.Row="5"
                   Text="{Binding Result}"
                   FontSize="16"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap" />
    </Grid>
</UserControl>
